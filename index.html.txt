<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Happy Birthday My Love 💖</title>

<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap" rel="stylesheet">

<style>
body {
    margin: 0;
    padding: 0;
    font-family: 'Orbitron', sans-serif;
    overflow: hidden;
    height: 100vh;
    background: linear-gradient(135deg, #0f0f0f, #1a001f, #000000);
    display: flex;
    justify-content: center;
    align-items: center;
}

/* Glass Effect Container */
.container {
    position: relative;
    padding: 40px 60px;
    text-align: center;
    border-radius: 20px;
    backdrop-filter: blur(15px);
    background: rgba(255, 45, 160, 0.08);
    border: 1px solid rgba(255, 45, 160, 0.3);
    box-shadow: 0 0 40px rgba(255, 45, 160, 0.4);
    color: #ff2da0;
    z-index: 2;
}

h1 {
    font-size: 2.8rem;
    letter-spacing: 4px;
    animation: glow 1.5s infinite alternate;
}

p {
    font-size: 1.1rem;
    color: #ff99dd;
}

@keyframes glow {
    from {
        text-shadow: 0 0 10px #ff2da0, 0 0 20px #ff2da0;
    }
    to {
        text-shadow: 0 0 20px #ff66cc, 0 0 40px #ff33aa;
    }
}

/* Floating Hearts */
.heart {
    position: absolute;
    bottom: -20px;
    font-size: 20px;
    color: #ff2da0;
    animation: floatUp linear infinite;
}

@keyframes floatUp {
    to {
        transform: translateY(-110vh);
        opacity: 0;
    }
}

/* Fireworks Canvas */
canvas {
    position: absolute;
    top: 0;
    left: 0;
}
</style>
</head>

<body>

<!-- Background Music -->
<audio autoplay loop>
    <source src="Ricky_Montgomery_-_Line_Without_a_Hook_(mp3.pm).mp3" type="audio/mpeg">
</audio>

<div class="container">
    <h1>Happy Birthday My Love 💖</h1>
    <p>You are my neon dream, my forever spark ✨</p>
    <p>Click anywhere for magic 🎆</p>
</div>

<canvas id="fireworks"></canvas>

<script>
/* Floating Hearts */
function createHeart() {
    const heart = document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML = "💖";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.animationDuration = (Math.random() * 3 + 3) + "s";
    document.body.appendChild(heart);
    setTimeout(() => { heart.remove(); }, 6000);
}
setInterval(createHeart, 500);

/* Fireworks */
const canvas = document.getElementById("fireworks");
const ctx = canvas.getContext("2d");

canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

let particles = [];

function createFirework(x, y) {
    for (let i = 0; i < 80; i++) {
        particles.push({
            x: x,
            y: y,
            radius: 2,
            color: "hsl(" + Math.random() * 360 + ", 100%, 60%)",
            speedX: (Math.random() - 0.5) * 8,
            speedY: (Math.random() - 0.5) * 8,
            life: 100
        });
    }
}

function animate() {
    ctx.fillStyle = "rgba(0, 0, 0, 0.2)";
    ctx.fillRect(0, 0, canvas.width, canvas.height);

    particles.forEach((p, index) => {
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.radius, 0, Math.PI * 2);
        ctx.fillStyle = p.color;
        ctx.fill();

        p.x += p.speedX;
        p.y += p.speedY;
        p.life--;

        if (p.life <= 0) particles.splice(index, 1);
    });

    requestAnimationFrame(animate);
}

canvas.addEventListener("click", (e) => {
    createFirework(e.clientX, e.clientY);
});

animate();
</script>

</body>
</html>
